type User {
  id: ID!
}
type Book {
  title: String
  authors: [User!]!
  author: User!
}

type Me {
  userID: String!
  books: [Book!]!
}

type Query {
  me: Me
}

type Mutation {
  addBook: Book!
}
