{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "local": "firebase emulators:exec --config=packages/functions/firebase.json --ui 'yarn workspace @posusume/api dev:watch'"
  }
}
